---
description: Build debug and launch HomeClaw
allowed-tools: Bash(scripts/*), Bash(open *)
---

Build HomeClaw in debug mode and launch it.

1. Run `scripts/build.sh --debug`
2. Launch the built app: `open .build/DerivedData/Build/Products/Debug-maccatalyst/HomeClaw.app`
