---
description: Build release, install to /Applications, and launch
allowed-tools: Bash(scripts/*), Bash(open *), Bash(ln *)
---

Build HomeClaw in release mode, install to /Applications, and launch.

1. Run `scripts/build.sh --release --install`
2. Launch: `open /Applications/HomeClaw.app`
